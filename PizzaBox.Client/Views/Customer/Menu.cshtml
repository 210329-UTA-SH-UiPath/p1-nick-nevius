@model PizzaBox.Client.Models.Customer


@{ 
// TODO:
// this should be changed to just use an action link and passing the customer name through it
}

@Html.ActionLink("Order History", "CustomerOrderHistory", "Order", new { Name = Model.Name })
@Html.ActionLink("New Order", "Select", "Store")